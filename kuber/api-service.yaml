apiVersion: v1
kind: Service
metadata:
  name: dify-api
spec:
  selector:
    app: dify-api
  ports:
  - protocol: TCP
    port: 5001  # Match container port
    targetPort: 5001
  type: LoadBalancer  # Expose within the cluster
