apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dify-storage-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Adjust storage size as needed
