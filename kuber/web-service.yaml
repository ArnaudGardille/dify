apiVersion: v1
kind: Service
metadata:
  name: dify-web
spec:
  selector:
    app: dify-web
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000 
  type: LoadBalancer # or NodePort, depending on your cloud setup
